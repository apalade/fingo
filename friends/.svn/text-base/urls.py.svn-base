from django.conf.urls.defaults import *

__author__="Alexandru Palade <alexandru.palade@loopback.ro>"
__date__ ="$${date} ${time}$"

# Uncomment the next two lines to enable the admin:
# from django.contrib import admin
# admin.autodiscover()

urlpatterns = patterns('friends.views',
    # Example:
    (r'^/?$', 'index', {}, 'friends-index'),
    (r'^/add$', 'add', {}, 'friends-add'),
    (r'^/confirm$', 'confirm', {}, 'friends-confirm'),
    (r'^/get$', 'get', {}, 'friends-get'),
    (r'^/invite$', 'invite', {}, 'friends-invite'),
)